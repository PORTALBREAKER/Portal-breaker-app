name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Java 21
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '21'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3

    - name: Prepare build environment and keystores
      run: |
        # Ensure debug.keystore exists in every expected location:
        # 1. Root folder
        # 2. app/ folder
        # 3. Default Android user home folder (~/.android/debug.keystore)
        
        mkdir -p ~/.android
        mkdir -p app

        if [ -f "debug.keystore.base64" ]; then
          base64 -d debug.keystore.base64 > debug.keystore
          cp debug.keystore app/debug.keystore
          cp debug.keystore ~/.android/debug.keystore
        elif [ -f "app/debug.keystore.base64" ]; then
          base64 -d app/debug.keystore.base64 > debug.keystore
          cp debug.keystore app/debug.keystore
          cp debug.keystore ~/.android/debug.keystore
        else
          # Fallback generate a fresh debug keystore if ever missing
          keytool -genkey -v -keystore debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"
          cp debug.keystore app/debug.keystore
          cp debug.keystore ~/.android/debug.keystore
        fi
        
        echo "Keystore verified at:"
        ls -la debug.keystore app/debug.keystore ~/.android/debug.keystore

        # Ensure empty .env exists for secrets plugin
        touch .env
        touch app/.env

        # Ensure gradlew has executable permission
        chmod +x gradlew

    - name: Build APK with Gradle
      env:
        ANDROID_HOME: /usr/local/lib/android/sdk
      run: |
        ./gradlew assembleDebug \
          --no-daemon \
          --no-configuration-cache \
          -Djava.awt.headless=true \
          -Pksp.useKSP2=true

    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: PortalBreaker-APK
        path: app/build/outputs/apk/debug/*.apk
        if-no-files-found: error
